import React from 'react';

const LeoSagittarius = React.memo(() => {
    return (
        <React.Fragment>
            <div> LeoSagittarius Component </div>
        </React.Fragment>
    );
});

export default LeoSagittarius;
