import React from 'react';

const LeoLibra = React.memo(() => {
    return (
        <React.Fragment>
            <div> LeoLibra Component </div>
        </React.Fragment>
    );
});

export default LeoLibra;
