import React from 'react';

const SagittariusCapricorn = React.memo(() => {
    return (
        <React.Fragment>
            <div> SagittariusCapricorn Component </div>
        </React.Fragment>
    );
});

export default SagittariusCapricorn;
