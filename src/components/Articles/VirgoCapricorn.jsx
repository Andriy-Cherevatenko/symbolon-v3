import React from 'react';

const VirgoCapricorn = React.memo(() => {
    return (
        <React.Fragment>
            <div> VirgoCapricorn Component </div>
        </React.Fragment>
    );
});

export default VirgoCapricorn;
